let cancer = () => {
    let temp = null;
    let doesNotExist = null;
    if (temp != doesNotExist) {
        return true;
    } else if (temp === doesNotExist) {
        return false
    }
}
 let temp = null;
let cancerAi = () => {

    let d = !!+[]; temp ^= d << 1;
    let doesNotExist = +[] >> d << d | +[];
    return !!((temp ^ doesNotExist) | (temp << doesNotExist));

};

let ebola = () => {
    let temp = null, d = !!+[]; temp ^= d << 1; doesNotExist = +[] >> d << d | +[];
     return !!(temp ^ doesNotExist) || !!temp + !!doesNotExist;

}
console.log(cancerAi());